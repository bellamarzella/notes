## Reflected XSS (Non-Persistent)

### What is it?
The payload is part of the request set to the server and is immediately reflected back in the server's HTTP response. It is non-persistent, meaning the payload has to be manually triggered.
### What might it look like?
Most often, reflected XSS occurs via URL query