## Reflected XSS (Non-Persistent)

### What is it?

The payload is part of the request set to the server, for example via URL query parameter or form submission, and is immediately reflected back in the server's HTTP response.
